# 验收记录

## 自动检查

2026-09-20：Python 83 项通过；最后一次进度修复后的 JavaScript 检查 34 项通过。覆盖 EPUB 解析、异常输入、章节边界、阅读进度、书库迁移、桌面服务归属、阅读设置和书架行为。构建检查包括本机签名、动态库依赖以及运行环境搬移后的导入。

## 桌面进度闭环

2026-09-21，在本机已安装的独立 Reader3.app 内完成实际操作：

1. 打开原创演示书，滚动到新的正文位置。
2. 页面显示“阅读位置已保存在本机”；本机记录的 scrollTop 为 250，段落标识为 r0-observe，段内偏移为 19.609375。
3. 正常退出应用，确认应用启动的本机服务也已停止。
4. 重新打开应用，从书架点击“继续阅读”。
5. 画面回到同一段；读取持久记录，章节、滚动位置、段落标识和段内偏移均一致；重新打开后的保存时间已更新。

此项确认滚动产生的新位置可跨应用退出／重开保存，不只检查已有记录能否被读取。本次不需要追加代码修复。

## 其他实际操作与边界

此前已验证本机导入、重复导入、整章及所选词经系统剪贴板粘贴、阅读设置、窄屏目录和独立应用启动。没有把向外部 AI 发送书籍算作验收，也没有验证所有 EPUB 或其他电脑。

后台窗口有意避免用自动布局产生的位置覆盖前台阅读进度。初始化等待动画帧时有有界回退；相关缺失接口、后台与迟到动画帧情形有自动检查。

## 纯源码发布包复核

2026-09-21：在不含个人书籍、运行环境、打包产物和阅读记录的发布目录运行检查：Python 共83项，82项通过，1项因目录不含编译后的桌面程序而跳过；JavaScript 34项通过。修正了路由检查依赖本机demo.epub的问题，改为在临时目录生成原创演示书。
